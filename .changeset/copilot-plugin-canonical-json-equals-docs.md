---
"@effected/copilot-plugin": patch
---

## Other

- `skills/effected-packages/references/schemastore.md` now documents `CanonicalJson.equals` — the parsed-content equality predicate with the serializer's own semantics — so agents comparing documents by content reach for the library helper instead of hand-rolling a deep-equal.
